# WatchApp

This folder is reserved for watch-app-specific resources if you later split the watch bundle and extension further.

The initial implementation keeps executable Swift sources in `WatchExtension/` and the shared watch target wiring in the generated Xcode project.
